// Package rpc provides the core RPC layer for courier: server-side service
// registration and dispatch, client-side request/response matching with
// timeout and retry, and an interceptor chain for cross-cutting concerns.
package rpc
